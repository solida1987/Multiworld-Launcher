<Application x:Class="LauncherV2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <!-- Global colour palette — change these to restyle the entire launcher.
             Values match the de-facto dark-blue/gold theme used by MainWindow
             and every hardcoded panel (#0D1018 bg / #141720 card / #1E2233
             border / #CCA800 gold / #CCD0E0 text / #727A99 muted). The old
             brown/parchment values painted the main column a different theme
             than the sidebar (P2-15). -->
        <Color x:Key="ColourBackground">#0D1018</Color>
        <Color x:Key="ColourPanel">#141720</Color>
        <Color x:Key="ColourAccent">#CCA800</Color>
        <Color x:Key="ColourText">#CCD0E0</Color>
        <Color x:Key="ColourMuted">#727A99</Color>
        <Color x:Key="ColourSuccess">#22C55E</Color>
        <Color x:Key="ColourError">#D94A4A</Color>

        <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColourBackground}"/>
        <SolidColorBrush x:Key="BrushPanel"      Color="{StaticResource ColourPanel}"/>
        <SolidColorBrush x:Key="BrushAccent"     Color="{StaticResource ColourAccent}"/>
        <SolidColorBrush x:Key="BrushText"       Color="{StaticResource ColourText}"/>
        <SolidColorBrush x:Key="BrushMuted"      Color="{StaticResource ColourMuted}"/>
        <SolidColorBrush x:Key="BrushSuccess"    Color="{StaticResource ColourSuccess}"/>
        <SolidColorBrush x:Key="BrushError"      Color="{StaticResource ColourError}"/>
        <SolidColorBrush x:Key="BrushControl"    Color="#1A1E30"/>
        <SolidColorBrush x:Key="BrushControlBorder" Color="#2A3050"/>
        <SolidColorBrush x:Key="BrushControlHover"  Color="#232A40"/>

        <!-- Default text style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="13"/>
        </Style>

        <!-- ============================================================
             Dark chrome for the stock controls.

             WHY: the implicit TextBlock style above paints every piece of
             text light. Controls that kept their DEFAULT Windows template
             (light grey buttons, white combo boxes, white menus, white
             tooltips) therefore rendered LIGHT text on a LIGHT surface —
             the "Save YAML…"/"Cancel" buttons and the Goal dropdown in the
             YAML dialog, and the Unbug Save menu, were all next to
             unreadable. ~80 files create controls in code; restyling them
             one by one is how this class of bug got shipped repeatedly, so
             the fix lives HERE, once, and every current and future window
             inherits it.

             HOW the templates stay compatible with the ~190 buttons that
             already set their own colours inline: every surface is a
             TemplateBinding, so a locally set Background/BorderBrush wins
             exactly as before; hover is a translucent overlay drawn ON TOP
             of whatever the background is; and the disabled state dims via
             Opacity instead of swapping Foreground, because the implicit
             TextBlock style would override any foreground a trigger sets
             (string content becomes a TextBlock INSIDE the template, and
             App-level implicit styles reach into templates for
             non-Control elements).
             ============================================================ -->

        <Style TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource BrushControl}"/>
            <Setter Property="Foreground"      Value="{StaticResource BrushText}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BrushControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="12,6"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Border x:Name="Overlay" Background="Transparent" CornerRadius="4"/>
                                <ContentPresenter x:Name="Content"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Overlay" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Overlay" Property="Background" Value="#33000000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome"  Property="Opacity" Value="0.55"/>
                                <Setter TargetName="Content" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            <Setter Property="Padding"    Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="3"
                                Padding="{TemplateBinding Padding}" Margin="2,1"
                                SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushControlHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A3352"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background"      Value="{StaticResource BrushControl}"/>
            <Setter Property="Foreground"      Value="{StaticResource BrushText}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BrushControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight"       Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                      RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd"
                                                Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                CornerRadius="4" SnapsToDevicePixels="True">
                                            <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="{StaticResource BrushMuted}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush"
                                                        Value="{StaticResource BrushMuted}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Margin="10,0,26,0" VerticalAlignment="Center"
                                              HorizontalAlignment="Left" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                                <Border Background="{StaticResource BrushPanel}"
                                        BorderBrush="{StaticResource BrushControlBorder}"
                                        BorderThickness="1" CornerRadius="4" Margin="0,3,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background"  Value="{StaticResource BrushPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushControlBorder}"/>
            <Setter Property="Foreground"  Value="{StaticResource BrushText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4" Padding="4"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Covers both roles a ContextMenu produces: plain items and items
             that open a submenu (the Unbug Save repair -> character tree). -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="3"
                                Padding="12,7" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Header" ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"/>
                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 0 8 Z"
                                      Fill="{StaticResource BrushMuted}" Visibility="Collapsed"
                                      VerticalAlignment="Center" Margin="14,0,0,0"/>
                                <Popup x:Name="PART_Popup" Placement="Right"
                                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                                    <Border Background="{StaticResource BrushPanel}"
                                            BorderBrush="{StaticResource BrushControlBorder}"
                                            BorderThickness="1" CornerRadius="4" Padding="4"
                                            Margin="2,0,0,0">
                                        <ItemsPresenter/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{StaticResource BrushControlHover}"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Header" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToolTip">
            <Setter Property="Background"  Value="{StaticResource BrushPanel}"/>
            <Setter Property="Foreground"  Value="{StaticResource BrushText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"     Value="10,6"/>
        </Style>
    </Application.Resources>
</Application>
