<Window x:Class="LauncherV2.UI.Controls.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multiworld Launcher"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- ── Primary action (gold; red when the action is destructive) ── -->
        <Style x:Key="DlgPrimaryStyle" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource BrushAccent}"/>
            <Setter Property="Foreground"      Value="#1A0800"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Padding"         Value="18,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Secondary action (No / Cancel) ── -->
        <Style x:Key="DlgSecondaryStyle" TargetType="Button">
            <Setter Property="Background"      Value="#1A1E30"/>
            <Setter Property="Foreground"      Value="{StaticResource BrushText}"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Padding"         Value="16,7"/>
            <Setter Property="BorderBrush"     Value="#2A3060"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A2040"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#141720"
            BorderBrush="#1E2233"
            BorderThickness="1"
            CornerRadius="6"
            MinWidth="380"
            MaxWidth="460"
            Margin="14"
            MouseLeftButtonDown="Card_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="24" Opacity="0.55" ShadowDepth="3"/>
        </Border.Effect>
        <StackPanel Margin="22,18,22,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock x:Name="TxtDlgIcon"
                           FontSize="15"
                           VerticalAlignment="Center"
                           Margin="0,0,9,0"/>
                <TextBlock x:Name="TxtDlgTitle"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="{StaticResource BrushText}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock x:Name="TxtDlgBody"
                       FontSize="12"
                       Foreground="{StaticResource BrushMuted}"
                       TextWrapping="Wrap"
                       LineHeight="18"
                       Margin="0,0,0,18"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnDlgYes"
                        Style="{StaticResource DlgPrimaryStyle}"
                        MinWidth="92"
                        IsDefault="True"
                        Click="BtnYes_Click"/>
                <Button x:Name="BtnDlgNo"
                        Style="{StaticResource DlgSecondaryStyle}"
                        MinWidth="92"
                        Margin="8,0,0,0"
                        Visibility="Collapsed"
                        Click="BtnNo_Click"/>
                <Button x:Name="BtnDlgCancel"
                        Style="{StaticResource DlgSecondaryStyle}"
                        MinWidth="80"
                        Margin="8,0,0,0"
                        IsCancel="True"
                        Visibility="Collapsed"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
